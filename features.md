# Features

이 문서는 생산성 앱의 주요 기능을 명세합니다. 각 기능은 목적, 입력, 출력, 처리 흐름, 예외 상황, 비고를 기준으로 정리합니다.

## 앱 등록 기능

### 목적

사용자가 사용 시간을 추적할 앱을 등록합니다. 모든 실행 프로세스를 추적하지 않고 사용자가 선택한 앱만 관리함으로써 노이즈를 줄이고 성능을 유지합니다.

### 입력

- 앱 이름
- 프로세스 이름
- 일일 제한 시간
- 알림 활성화 여부

### 출력

- `trackedApps`에 등록된 앱 정보
- Main Process에 전달되는 추적 대상 목록

### 처리 흐름

사용자가 앱을 등록하면 Renderer Process의 Zustand store가 `trackedApps`를 갱신합니다. 변경된 목록은 localStorage에 저장되고, IPC를 통해 Main Process에 전달됩니다.

Main Process는 다음 추적 주기부터 새 앱을 감지 대상에 포함합니다. 이 흐름을 통해 UI에서 등록한 설정과 백그라운드 추적 기준이 동일하게 유지됩니다.

### 예외 상황

- 프로세스 이름이 비어 있으면 등록하지 않습니다.
- 이미 등록된 프로세스 이름이면 중복 등록을 막습니다.
- 제한 시간이 0 이하이면 기본값을 적용하거나 저장을 거부합니다.
- OS별 프로세스 이름이 다른 경우 사용자에게 확인 가능한 형태로 표시합니다.

### 비고

프로세스 이름은 OS별로 다를 수 있으므로 장기적으로는 앱 선택 UI와 OS별 감지 모듈을 분리하는 것이 좋습니다.

## 사용 시간 측정

### 목적

등록된 앱이 실행 중인 시간을 일자별로 누적해 사용자가 자신의 앱 사용 패턴을 확인할 수 있게 합니다.

### 입력

- 현재 실행 중인 프로세스 목록
- `trackedApps`
- 현재 날짜
- tracking interval

### 출력

- 앱별 누적 사용 시간
- `usageTimes[date][appId]`

### 처리 흐름

Main Process가 일정 주기로 실행 중인 프로세스를 조회합니다. 등록된 앱의 `processName`이 실행 목록에 포함되어 있으면 해당 앱의 사용 시간을 증가시킵니다.

변경된 사용 시간은 IPC로 Renderer Process에 전달되고 Zustand store에 반영됩니다. Renderer는 이 값을 대시보드와 통계 화면에 실시간으로 표시합니다.

### 예외 상황

- 프로세스 조회 실패 시 해당 tick은 누적하지 않고 다음 tick에서 재시도합니다.
- 날짜가 변경되면 새로운 날짜 키로 사용 시간을 기록합니다.
- 앱이 종료되면 사용 시간 누적을 중단합니다.
- 추적 interval이 지연된 경우 실제 경과 시간 기준으로 보정할 수 있습니다.

### 비고

정확도와 성능의 균형을 위해 기본 추적 주기는 1초로 둡니다. 다만 UI 저장은 debounce 또는 throttle을 적용해 localStorage 쓰기 비용을 줄일 수 있습니다.

## 제한 시간 설정

### 목적

사용자가 앱별 일일 사용 제한 시간을 설정해 과도한 사용을 인지할 수 있게 합니다.

### 입력

- `appId`
- `dailyLimitMinutes`
- `notificationEnabled`

### 출력

- 갱신된 `trackedApps`
- Main Process의 제한 시간 판단 기준

### 처리 흐름

Renderer Process에서 제한 시간을 변경하면 Zustand store가 설정을 반영합니다. 이후 IPC로 Main Process에 설정 전체 또는 변경분을 전달합니다.

Main Process는 누적 시간이 제한 시간을 초과하는지 매 tick마다 확인합니다. 제한 시간을 초과하면 알림 시스템으로 이벤트를 전달합니다.

### 예외 상황

- 제한 시간이 비어 있으면 기본값을 사용합니다.
- 잘못된 숫자 입력은 저장하지 않습니다.
- 제한 시간이 변경되었을 때 이미 초과 상태라면 즉시 알림 여부를 재평가합니다.
- 알림이 비활성화된 앱은 초과 상태여도 데스크톱 알림을 보내지 않습니다.

### 비고

현재 버전은 앱 강제 종료를 하지 않습니다. 제한 시간은 알림 기준으로만 사용합니다.

## 알림 시스템

### 목적

앱 사용 시간이 제한 시간을 초과했을 때 사용자가 즉시 인지할 수 있도록 데스크톱 알림을 제공합니다.

### 입력

- 앱별 누적 사용 시간
- 앱별 제한 시간
- 알림 활성화 여부
- 알림 발송 이력

### 출력

- Electron 데스크톱 알림
- `notifications` 기록

### 처리 흐름

Main Process는 사용 시간이 제한 시간을 넘었는지 확인합니다. 조건을 만족하고 해당 날짜에 같은 앱으로 알림을 보낸 기록이 없으면 Electron Notification API를 호출합니다.

알림 발생 정보는 IPC를 통해 Renderer Process에 전달되고 store에 저장됩니다. 저장된 이력은 중복 알림 방지와 사용자 확인용 히스토리에 사용됩니다.

### 예외 상황

- OS 알림 권한이 비활성화된 경우 UI에 상태를 표시합니다.
- 알림 API 호출 실패 시 로그를 남기고 추적은 계속합니다.
- 중복 알림은 날짜와 `appId` 기준으로 차단합니다.
- 앱 전체 알림 설정이 꺼져 있으면 알림을 발송하지 않습니다.

### 비고

알림은 사용자 경험에 직접 영향을 주므로 반복 발송을 피하고, 설정에서 비활성화할 수 있어야 합니다.

## 사용 통계

### 목적

사용자가 하루 동안 어떤 앱을 얼마나 사용했는지 확인할 수 있도록 일간 통계를 제공합니다.

### 입력

- `usageTimes`
- `trackedApps`
- 선택된 날짜

### 출력

- 앱별 사용 시간
- 총 사용 시간
- 제한 시간 대비 사용률
- 제한 시간 초과 여부

### 처리 흐름

Renderer Process는 Zustand store의 `usageTimes`를 기반으로 통계를 계산합니다. 앱 이름, 제한 시간, 사용 시간을 조합해 대시보드에 표시합니다.

통계 계산은 UI 컴포넌트에 직접 넣기보다 selector 또는 별도 utility로 분리합니다. 이렇게 하면 화면 변경과 계산 로직 변경을 독립적으로 관리할 수 있습니다.

### 예외 상황

- 해당 날짜 데이터가 없으면 빈 상태를 표시합니다.
- 등록 해제된 앱의 과거 데이터는 통계에서 보존하거나 별도 표시합니다.
- 날짜 변경 시 현재 날짜의 통계로 자동 전환합니다.
- localStorage 데이터가 손상된 경우 기본 통계 상태로 복구합니다.

### 비고

통계 계산은 selector로 분리해 렌더링 비용을 줄입니다. 장기 통계나 복잡한 집계가 필요해지면 SQLite 기반 저장소로 이전하는 것이 적합합니다.

## 데이터 저장

### 목적

앱을 종료하거나 새로고침해도 등록 앱, 설정, 사용 시간, 알림 기록을 유지합니다.

### 입력

- `trackedApps`
- `usageTimes`
- `settings`
- `notifications`

### 출력

- localStorage에 직렬화된 상태 데이터
- 앱 시작 시 복원된 Zustand 상태

### 처리 흐름

Renderer Process에서 Zustand persist 또는 storage adapter를 사용해 상태를 localStorage에 저장합니다. 앱 시작 시 저장된 데이터를 복원하고, 복원된 `trackedApps`와 `settings`를 Main Process에 IPC로 전달합니다.

Main Process는 전달받은 설정을 기준으로 추적 루프를 시작하거나 갱신합니다.

### 예외 상황

- localStorage 데이터가 손상되면 기본 상태로 복구합니다.
- 저장 용량을 초과하면 오래된 알림 기록 또는 통계 데이터를 정리합니다.
- 상태 schema가 변경되면 version 기반 migration을 적용합니다.
- 저장 실패가 발생해도 현재 메모리 상태와 추적 루프는 유지합니다.

### 비고

localStorage는 MVP에 적합하지만 장기 통계에는 한계가 있습니다. SQLite 이전을 고려해 저장 로직은 UI 컴포넌트에서 직접 호출하지 않습니다.
